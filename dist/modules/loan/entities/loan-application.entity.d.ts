import { Model } from "sequelize-typescript";
import { User } from "../../user/entities/user.entity";
import { Document } from "../../document/entities/document.entity";
export type LoanStatus = "NEW_LEAD" | "UNDER_REVIEW" | "STIPS_NEEDED" | "OFFER_SENT" | "DECLINED" | "FUNDED";
export declare class LoanApplication extends Model<LoanApplication> {
    id: string;
    userId: string | null;
    user: User;
    requestedAmount: number;
    annualRevenue: number;
    timeInBusiness: number;
    businessName: string;
    einEncrypted: string | null;
    industry: string;
    businessAddress: string;
    businessCity: string;
    businessState: string;
    businessZip: string;
    documents: Document[];
    ownerName: string;
    ssnEncrypted: string | null;
    phone: string;
    email: string;
    dateOfBirth: string;
    status: LoanStatus;
    adminNote: string | null;
    offeredAmount: number | null;
    factorRate: number | null;
    paymentFrequency: string | null;
    termMonths: number | null;
    totalPayback: number | null;
    tier: "express" | "high_limit";
    pdfUrl: string | null;
}
